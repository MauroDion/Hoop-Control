// This file is intentionally left blank to prevent conflicts with the root next.config.ts.
// The primary configuration for Next.js should be in the root of the project.
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
