
"use client";

// This component is intentionally disabled to avoid confusion with Firebase project linking.
// The user is hardcoded in AuthContext, so a user menu is not necessary for development.
export function UserNav() {
  return null;
}
